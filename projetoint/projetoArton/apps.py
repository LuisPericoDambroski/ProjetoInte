from django.apps import AppConfig

class ProjetoArtonConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'projetoArton'  # Nome deve ser exatamente como está no INSTALLED_APPS